# V2 測定報告 — NCCL チャネル数と JIT キャッシュ永続化

この文書は、二台サービングレシピ([README.ja.md](README.ja.md) /
[AGENTS.md](AGENTS.md) の構成)の serve スクリプトに私が足した二つの任意
環境変数 `NCCL_NCHANNELS` と `JIT_CACHE_DIR` について、何を変え、どの
条件で何を測り、どの数値を得たかの記録です。私が測った構成・入力・順序の
範囲に限定した実測報告であり、それを超える性質は主張しません。

## 変えたもの

`serve/start-head.sh`、`serve/start-worker.sh`、`serve/nccl-ib.sh` に、
二つの任意の口を足しました。どちらも環境変数です。**両方とも未設定の
ままなら、コンテナへ渡す引数は前の版と完全に同一で、振る舞いも前の版と
同じです。**

- `NCCL_NCHANNELS` — `NCCL_IB=1`(RDMA 派生イメージ)の経路でのみ有効。
  設定すると、コンテナへ `NCCL_MAX_NCHANNELS` と
  `NCCL_MAX_P2P_NCHANNELS` として渡され、NCCL のチャネル数を抑えます。
  NCCL はチャネルごとのバッファを、エンジンがサービングに使うのと同じ
  ユニファイドメモリから確保するので、チャネル数はホストメモリの
  取り分に効きます。未設定なら NCCL の既定(この環境では 64)のままです。
  `NCCL_IB=0`(sockets)ではこの変数は効きません。
- `JIT_CACHE_DIR` — ホスト上の絶対パス。設定するとそのディレクトリが
  `/jit-cache` にマウントされ、`TRITON_CACHE_DIR`、
  `TORCHINDUCTOR_CACHE_DIR`、`FLASHINFER_WORKSPACE_DIR`、
  `VLLM_CACHE_ROOT` の向き先がその配下へ変わります。未設定なら従来
  どおり、起動のたびにコンテナ層へコンパイルして `docker rm` とともに
  捨てます。

## この構成を選ぶ手順

変数はノードごとの `serve/serve.env` に書きます。操作しているシェルで
export しても ssh 先へ自動では渡らないので、**各ノードのファイルに**
書いてください。

head ノードの `serve/serve.env` に、既存の `HEAD_IP` / `IFNAME` /
`MODEL_DIR` などへ追加する形で:

```bash
export NCCL_NCHANNELS=8
export JIT_CACHE_DIR=/abs/path/on/this/node/jit-cache
```

worker ノードの `serve/serve.env` にも同じ二行を足します(worker 側は
`MY_IP` も必要です)。`JIT_CACHE_DIR` は**そのノード上の**書き込める
絶対パスを指してください。キャッシュの中身はノードごとにローカルに
生成されます。スクリプトが `mkdir -p` しますが、相対パスは拒否されます。
TP=2 の rank 1 は worker ノードで動くので、worker 側にも設定が要ります。

あとの手順は従来どおりです。各ノードで `. serve/serve.env` を読み込み、
head を先に `serve/start-head.sh` で起動し、head の Ray ポート(6399)が
開くのを待ってから worker を `serve/start-worker.sh` で起動します。
詳細は [AGENTS.md](AGENTS.md) のセクション 5 にあります。

起動後、設定が効いたことを確認します:

```bash
docker logs glm53-head 2>&1 | grep -i channel     # チャネル数が 8 になっていること
docker inspect glm53-head | grep -e NCCL_MAX -e jit-cache
# worker 側はコンテナ名 glm53-worker
```

`JIT_CACHE_DIR` の効果が出るのは、キャッシュが一度書き込まれた後の
起動からです。初回の起動は従来どおりコンパイルが走ります。

## 測った条件

- 私のノードペア(head / worker の二台)で、同一の `serve.env`、同一の
  serve 行・モデル・オーバーレイ。比較のために変えたのはチャネル数だけ
  です(64 = `NCCL_NCHANNELS` 未設定の既定、8 = `NCCL_NCHANNELS=8`)。
- 速度・並列の比較では、既存の速度比較記録
  (log名 `lab2-pair1`) の各起動を使い、
  `--prefix-tokens 2000`、`--max-tokens 256`、`--runs 3` の中央値、
  temperature=0、ignore-eos、thinking=off で測りました。両条件とも
  JIT永続化を有効にした起動です。
- 同時実行数は 1 → 2 → 4 → 8 の順に測りました。
- 長文脈の梯子は、NCCL8の単独起動で、JIT永続化を追加する前の測定です。
  既存ルーラー文の反復、temperature=0、max_tokens=16、短い順に温めた
  起動で昇順に流しました。この起動は12,225 MiBのJIT warm起動の長文
  試験とは別のものです。

## 結果

### 速度と並列

合計 tok/s(測定値そのもの):

| 同時実行 | NCCL 8 | NCCL 64 |
|---:|---:|---:|
| 1 | 28.7 | 29.0 |
| 2 | 36.9 | 36.8 |
| 4 | 45.4 | 44.9 |
| 8 | 55.9 | 54.9 |

- 失敗ストリームは全段・両設定とも 0 で、どちらの設定もこの測定中には
  落ちませんでした。
- TTFT が設定間で差を持ったのは同時実行 2 で、NCCL 8 のほうが
  851 ms 速い値でした。ほかの段では 56／91／204 ms の差で、
  同時実行 2 の差が最大でした。
- **測定した条件では、速度面のペナルティは見つかりませんでした。**
  見つからなかった条件は上に書いたとおりです——`--prefix-tokens 2000`・
  `--max-tokens 256` の固定入力長、同時実行 8 まで、両条件ともJIT永続化
  を有効にした各起動、です。JITを両方有効にしたことは、JIT／prefix／
  allocatorを含む全キャッシュ状態が同一であることの証明ではありません。
  確認できたのは操作条件の記載の範囲です。

### READY 時点のホストメモリ

ホストの OOM リーパーが見る量(空きメモリ、MemAvailable)を READY 直後に
測りました。

| 指標 | NCCL 8 | NCCL 64 |
|---|---:|---:|
| READY 直後の空き(head ノード) | 12,225 MiB | 8,770 MiB |
| READY 時点の KV キャッシュ | 4.18 GiB | 4.16 GiB |

この2起動の比較では、headのREADY空きはNCCL8の方が**+3,455 MiB**多い値でした。
TP1のKV予算は4.18／4.16 GiB、TP0は6.66／7.63 GiBでした。チャネル削減による
余裕改善を支持する結果ですが、起動ごとの変動を分離した効果量ではありません。
同時実行8の測定後にも、差は **+1,728 MiB** 残っていました。

12,225 MiBは上の対照比較に使った起動の測定値です。別の起動や運用全般の
代表値・保証値としては扱いません。同じスクリプト・同じノードでも
起動ごとに約 **1,700 MiB** のばらつきがあるので、この約1,700 MiBという
旧構成の観測幅を、新構成にも普遍的な変動幅として当てはめません。

### 別のペアでの再現

チャネル数の比較を、別の二台・別の運用者・別に書かれた測定ドライバ
で繰り返しました。起動はチャネル数以外の設定を揃えたもので、測定は短い入力から長い入力への昇順
の梯子を約 86k トークンまで流しました。

| ノード | 設定 | READY 直後の空き | 梯子中の最小値 |
|---|---|---:|---:|
| A | NCCL 64 | 10,133 MiB | 5,164 MiB |
| A | NCCL 8 | 11,798 MiB | 7,156 MiB |
| B | NCCL 64 | 11,463 MiB | 6,386 MiB |
| B | NCCL 8 | 13,539 MiB | 7,910 MiB |

- 最小値同士の差は、ノード A で **+1,992 MiB**、ノード B で
  **+1,524 MiB** です(どちらも NCCL 8 のほうが高い)。READY 直後
  同士の差はノード A で +1,665 MiB、ノード B で +2,076 MiB です。
- NCCL 8 の起動では、エンジンがプロファイルしたキャッシュプールも
  大きくなりました(419,672 対 376,026 トークン)。
- 中断もリーパーのイベントもなく、エンジンはこの測定の範囲では
  両方の走行を生き延びました。

増分は最初のペアで測った **+3,455 MiB**(READY 時点)より小さい
結果で、二つのペアは開始時点の余裕が異なります。この節の値は、
別のハードウェアで**方向が再現した**ことにあり、大きさが一致した
ことにはありません。

### 起動時間(JIT キャッシュ永続化)

| 指標 | 永続化なし | 永続化あり |
|---|---:|---:|
| READY まで | 684 s | 582 s |
| JIT kernel warmup | 3.22 s | 0.25 s |

メモリへの効果は、起動ごとのばらつき(約 1,700 MiB)と分離できません
でした。**この変更は起動時間の変化としてのみ報告します。**

このwarm起動ではJIT kernel warmupは 0.21 s と 0.31 s でした(冷態の
3.22 s に対して)。また、観測した20分間にキャッシュへの新規書込みは
見られませんでした。キャッシュ再利用と整合する結果として記録します。
メモリへの効果は起動ごとのばらつきと分離していません。

### 長文入力197,486トークンまでの昇順試験

宣言コンテキスト窓 204,800 に対し、197,486 トークンの入力まで行使
しました。その時点の head ノードの空きメモリは **6,544 MiB** でした。

- 65k → 86k トークンで **+1,704 MiB** の追加消費がありました。
- 86k より上(128k、197k)では高水位を更新しませんでした。

これは**昇順の順序で、一つのエンジンインスタンス上で**観測した結果
です。入力長一般の性質としては主張しません——ほかの順序は測って
いません。

## 設定の定着と起動の照合

両変数は各ノードの `serve/serve.env` に書き込んであり、通常の起動
(各ノードで env を読み込んで `start-head.sh` / `start-worker.sh`
を動かす)でこの構成が選ばれます。起動ごとに手で渡す引数はありま
せん。

### 起動したコンテナの照合

`serve/verify-serve.sh <コンテナ名> <envファイル>` は、起動済み
コンテナの `docker inspect` の内容——イメージ、コマンド行、環境
変数、マウント——を、env ファイルが宣言する内容と照合します。
照合するのは、イメージ(`IMAGE` / `NCCL_IB` / `NCCL_IB_IMAGE`
から決まる方)、コンテナ名、`/checkpoint` と `/checkpoint-mtp` の
マウント元、`OVERLAY_DIR` 配下からのオーバーレイマウント、
`RAY_memory_usage_threshold=0.99`、ノードの同一性
(`VLLM_HOST_IP` / `--node-ip-address` 対 `HEAD_IP` / `MY_IP`、
`NCCL_SOCKET_IFNAME` / `GLOO_SOCKET_IFNAME` 対 `IFNAME`)、
そして head コンテナでは serve 行の各フラグ
(`--served-model-name`、`--port`、`--tensor-parallel-size`、
`--max-model-len`、`--max-num-seqs`、`--gpu-memory-utilization`、
expert-parallel が付いていないこと、`--speculative-config` と
`MTP_K`)です。不一致は `MISMATCH:`、env が名付けるのにコンテナ
が公開しない項目は `UNVERIFIABLE:` と各行に出し、最後に件数を
`RESULT:` 行で報告します。`MISMATCH` が一つでもあれば終了
ステータスは 1 で、`UNVERIFIABLE` だけでは失敗になりません。

worker コンテナは `vllm serve` のコマンド行を持たない(ray へ
参加して待機するだけ)ので、serve 時の 9 項目は `UNVERIFIABLE`
と報告されます。これは worker の構成上予期される出力であり、
照合スクリプトの欠陥ではありません。照合対象は宣言された全変数を
網羅してはいません——たとえば `NCCL_NCHANNELS` 由来の
`NCCL_MAX_*` と `/jit-cache` マウントは対象外です。

### 配布上の隙間

配備中に見つかった現プロセスの限界です。四ノードのうち二ノード
で、置かれていたスクリプトがリポジトリより古く、両変数を欠いて
いました(残り二ノードは一致していました)。起動前に最新へ更新
しました。**ファイルは手で配布しており、ノード上のスクリプトが
現在版かを検証する仕組みはありません。** `verify-serve.sh` が
照合するのは起動済みのコンテナであり、ノードに置かれたスクリプト
の版ではありません。この隙間は埋まっていません。

## 測っていないもの

- 持続的な実トラフィック(実運用の負荷)
- 並列負荷下での、上記以外の入力長
- 降順、およびウォームアップなしの単発(単一ショット)の順序
- 二組目のペアでの、約 86k トークンを超える入力
- チャネル数による増分の大きさのハードウェア間での一般性
  (再現を確認したのは方向です)

## このレシピに含まれないもの、含まれるものの性質

- 測定中に運用者自身のマシン上で動かしていた内部向けの監視は、この
  レシピの同梱機能ではありません。それは通知と記録だけを行うもので、
  要求やエンジンを止めるものではなく、何も防ぎません。
- 同梱の `serve/check-headroom.sh` は診断上の目安です。安全の保証では
  ありません。

## 実負荷での観測（9 並列の agent 作業、33 分）

合成の梯子ではない実負荷を載せた機会があったので、その観測を記録します。
9 個の agent ジョブ（ソースファイルを読み、編集する作業。入力が長く出力が
短い）を、二つのペアに分けて 33 分動かしました。各ノードの空きメモリ
（MemAvailable、MiB）は次のとおりです。

| 時刻 | node1 | node2 | node3 | node4 |
|---|---:|---:|---:|---:|
| 01:35 | 4,435 | 4,807 | 3,260 | 4,739 |
| 01:42 | 4,424 | 4,819 | 3,230 | 4,729 |
| 01:49 | 4,402 | 4,803 | 3,235 | 4,729 |
| 02:08 | 3,299 | 3,671 | 3,251 | 4,687 |

READY 時点では 4 ノードが 12,192 / 13,661 / 11,941 / 12,840 MiB
でした。

- 掲載した4時点のMemAvailableは、各ノードのREADY時点より **7,757〜9,990 MiB** 少ない値でした。これは別起動の合成梯子で記録した最大減少 **5,526 MiB** より大きい観測です。ノード全体の変化であり、実負荷単独の消費量や生涯需要の上限としては扱いません。
- node3 は 33 分間フラットでした（**3,260 → 3,251**）。node1 と node2 は
  02:00 前後に約 **1,100 MiB** 下がって、その後ふたたびフラットに
  なりました。ここまでを観測された挙動として述べます。新しい作業と
  同時に起きた下落は、大きい形状を初めて見たときにメモリを確保すると
  いう以前の知見と**整合する**、までは言えます。因果と言うことは
  しません。下落の帰属を調べる計測はしていません。
- どのノードもリーパーの閾値を越えず、4 ノードとも 33 分間生き延びました。
- 逐次ドラフト（speculative decoding）はこの負荷下で動き続けました:
  平均受入長 **2.26〜2.61**（最大 3 に対して）、ドラフト受入率
  **68.6〜80.3 パーセント**。
- 生成スループットはエンジンあたり **13.8〜30.4 tok/s** で、プロンプト
  処理は **732〜1,841 tok/s** でした。この作業はプロンプト処理が
  支配的で、生成ではありません。これらをこの構成のスループットとは
  提示しません。この作業での数値です。

測っていないこと: 33 分は耐久の結果ではありません。同時実行 8 を超える
並列は測っていません。下落の帰属はしていません。ほかの作業形状は
試していません。
